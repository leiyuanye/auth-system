<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.auth.mapper.PhoneRealnameMapper">

    <resultMap id="PhoneRealnameMap" type="com.example.auth.entity.PhoneRealname">
        <id column="id" property="id"/>
        <result column="real_name" property="realName"/>
        <result column="id_card" property="idCard"/>
        <result column="phone" property="phone"/>
        <result column="department" property="department"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- ============ 查询 ============ -->
    <select id="selectAll" resultMap="PhoneRealnameMap">
        SELECT id, real_name, id_card, phone, department, status, remark, create_time
        FROM phone_realname
        ORDER BY id DESC
    </select>

    <select id="selectById" resultMap="PhoneRealnameMap">
        SELECT id, real_name, id_card, phone, department, status, remark, create_time
        FROM phone_realname
        WHERE id = #{id}
    </select>

    <select id="selectByCondition" resultMap="PhoneRealnameMap">
        SELECT id, real_name, id_card, phone, department, status, remark, create_time
        FROM phone_realname
        <where>
            <if test="keyword != null and keyword != ''">
                AND (real_name LIKE CONCAT('%', #{keyword}, '%') OR phone LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id DESC
        <if test="offset != null and limit != null">
            LIMIT #{offset}, #{limit}
        </if>
    </select>

    <select id="countByCondition" resultType="int">
        SELECT COUNT(*) FROM phone_realname
        <where>
            <if test="keyword != null and keyword != ''">
                AND (real_name LIKE CONCAT('%', #{keyword}, '%') OR phone LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <!-- ============ 新增 ============ -->
    <insert id="insert" parameterType="com.example.auth.entity.PhoneRealname" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO phone_realname (real_name, id_card, phone, department, status, remark, create_time)
        VALUES (#{realName}, #{idCard}, #{phone}, #{department}, #{status}, #{remark}, NOW())
    </insert>

    <!-- ============ 更新 ============ -->
    <update id="update" parameterType="com.example.auth.entity.PhoneRealname">
        UPDATE phone_realname
        <set>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="idCard != null">id_card = #{idCard},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="department != null">department = #{department},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- ============ 删除 ============ -->
    <delete id="deleteById">
        DELETE FROM phone_realname WHERE id = #{id}
    </delete>

</mapper>
