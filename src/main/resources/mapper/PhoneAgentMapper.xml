<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.auth.mapper.PhoneAgentMapper">

    <resultMap id="PhoneAgentMap" type="com.example.auth.entity.PhoneAgent">
        <id column="id" property="id"/>
        <result column="agent_name" property="agentName"/>
        <result column="contact" property="contact"/>
        <result column="phone" property="phone"/>
        <result column="address" property="address"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectAll" resultMap="PhoneAgentMap">
        SELECT id, agent_name, contact, phone, address, status, remark, create_time
        FROM phone_agent
        ORDER BY id DESC
    </select>

    <select id="selectById" resultMap="PhoneAgentMap">
        SELECT id, agent_name, contact, phone, address, status, remark, create_time
        FROM phone_agent
        WHERE id = #{id}
    </select>

    <select id="selectByCondition" resultMap="PhoneAgentMap">
        SELECT id, agent_name, contact, phone, address, status, remark, create_time
        FROM phone_agent
        <where>
            <if test="keyword != null and keyword != ''">
                AND (agent_name LIKE CONCAT('%', #{keyword}, '%') OR contact LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id DESC
        <if test="offset != null and limit != null">
            LIMIT #{offset}, #{limit}
        </if>
    </select>

    <select id="countByCondition" resultType="int">
        SELECT COUNT(*) FROM phone_agent
        <where>
            <if test="keyword != null and keyword != ''">
                AND (agent_name LIKE CONCAT('%', #{keyword}, '%') OR contact LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <insert id="insert" parameterType="com.example.auth.entity.PhoneAgent" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO phone_agent (agent_name, contact, phone, address, status, remark, create_time)
        VALUES (#{agentName}, #{contact}, #{phone}, #{address}, #{status}, #{remark}, NOW())
    </insert>

    <update id="update" parameterType="com.example.auth.entity.PhoneAgent">
        UPDATE phone_agent
        <set>
            <if test="agentName != null">agent_name = #{agentName},</if>
            <if test="contact != null">contact = #{contact},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="address != null">address = #{address},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM phone_agent WHERE id = #{id}
    </delete>

</mapper>
