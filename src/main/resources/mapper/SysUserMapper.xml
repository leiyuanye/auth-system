<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.auth.mapper.SysUserMapper">

    <resultMap id="SysUserMap" type="com.example.auth.entity.SysUser">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByUsername" resultMap="SysUserMap">
        SELECT id, username, password, real_name, email, phone, status, create_time, update_time
        FROM sys_user
        WHERE username = #{username}
          AND status = 1
        LIMIT 1
    </select>

    <select id="selectAll" resultMap="SysUserMap">
        SELECT id,username,real_name,email,phone,status,create_time
        FROM sys_user
    </select>

    <select id="selectById" resultMap="SysUserMap">
        SELECT id,username,real_name,email,phone,status
        FROM sys_user
        WHERE id=#{id}
    </select>

</mapper>
